<%= stylesheet_link_tag 'senddocument' %>

<h1>Поиск документа:</h1>

<div id="form_finder_document" style="margin:0 0 20px 0;">
        <form action="" method="post" class="form_find_documents">
                <div class="date">
                        <p>
                                <label class="required">По дате:</label>
                                с: 
                                <input type="text" name="by_date_start" value="<%= @doc_params['start_date'] %>" placeholder="Например: 01.01.2011" />
                                по: 
                                <input type="text" name="by_date_finish" value="<%= @doc_params['end_date'] %>" placeholder="Например: 01.01.2012" />
                        </p>
                </div>
                <div>
                        <p>
                                <label class="required">По типу:</label>
                                <select name="by_type">
                                        <option value=""></option>
                                        <% @typesList.each do |thisType| %>
                                                <option value="<%= thisType['id'] %>" <%= @doc_params['sended_form_type'] ==  thisType['id'] ? "selected" : ""  %> >
                                                        <%= thisType['name'] %>
                                                </option>
                                        <% end %>
                                </select>
                        </p>
                </div>
                <div>
                        <p>
                                <label class="required">По наименованию документа:</label>
                                <input type="text" name="document_name" value="<%= @doc_params['name'] %>" />
                        </p>
                </div>
                <div>
                        <p>
                                <label class="required">По отправителю:</label>
                                <select name="by_sender">
                                        <option value=""></option>
                                        <% @senderList.each do |thisSender| %>
                                                <option value="<%= thisSender['sender_email_id'] %>" <%= @doc_params['sender'] !=  thisSender['sender_email_id'] ? "" : "selected"  %> >
                                                        <%= thisSender['email'] %>
                                                </option>
                                        <% end %>
                                </select>
                        </p>
                </div>
                <div class="submit">
                        <p>
                                <label>&nbsp;</label>
                                <input type="submit" id="ffd_submit" value="Искать">
                        </p>
                </div>
        </form>
</div>

<% if @documentList != [] %>
<table cellpadding="0" cellspacing="0" border="0" class="resource">
        <thead>
        <tr>
                <td>Дата отправки</td>
                <td class="sendname">Наименование отправки</td>
                <td>Тип</td>
                <td style="white-space:nowrap;">Период(год, квартал)</td>
                <td>Ник отправителя</td>
                <td>Статус</td>
                <td>Комментарий клиентского центра</td>
        </tr>
        </thead>
        <tbody>
                <% @documentList.each do |column| %>
                        <%
                                        public_date = column['sended_date'];
                        %>
                        <tr class='<%= cycle ('odd', 'even') %>' >
                                <td>
                                        <%=public_date[8..9] %>.<%=public_date[5..6] %>.<%=public_date[0..3] %>
                                </td>
                                <td><%= column['name'] %></td>
                                <td><%= column['sended_form_type.name'] %></td>
                                <td><%= column['sended_form_date'] %></td>
                                <td><%= column['sender'] %></td>
                                <td><%= column['status'] %></td>
                                <td>
                                        <%= column['note'] != "" ? column['note'] : "-"  %>
                                </td>
                        </tr>
                <% end %>
        </tbody>
</table>

<div style="font-size:10px;">
        **Статусы: отправлен, принят, на согласовании, не согласован, требуется печатная копию.
</div>
<% else %>
Документов нет
<% end %>